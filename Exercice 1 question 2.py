mytable = [54,64,28,96,16]
valeurMin = mytable[0]
sauvegarde=0
valeur0=0
valeur1=0
plusgrandElement=0







for i in range(len(mytable)):
   while(i):
        (plusgrandElement < mytable[i])
        plusgrandElement = mytable[i]
        mytable.append(len(i))


    



input()

#le tri a bulles est beaucoup plus lent puisqu'il doit trouvé un a un qui est plus grand que qui
#pour ensuite le mettre a la fin et recommencer. Avec un système de tric classique cela irais 
# beaucoup plus rapidement puisqu'il n'y aurais pas besoin de chercher un a un lequel mettre en dernier
#ainsi de suite. Dans un très grand tableaux cela peut prend plusieurs seconde de tout avoir.
# avec un système autre cela prend quelque centieme de seconde.